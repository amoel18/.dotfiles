# Default config for sway
#
# Copy this to ~/.config/sway/config and edit it to your liking.
#
# Read `man 5 sway` for a complete reference.

### Variables
#
# Logo key. Use Mod1 for Alt.
set $mod Mod4
# Home row direction keys, like vim
set $left h
set $down j
set $up k
set $right l
# Your preferred terminal emulator
set $term alacritty
set $editor nvim
set $browser chromium
set $fuzz ~/.scripts/fuzzyf.sh
set $lock ~/.local/bin/scripts/lock.sh
set $power ~/.config/rofi/modi/power
set $wifi ~/.config/rofi/modi/nmcli
set $wifi-connect exec bash wofi-wifi-menu
set $screenshot grim ~/Images/screenshots/scrn-$(date +"%Y-%m-%d-%H-%M-%S").png
set $screenclip slurp | grim -g - ~/Images/screenshots/scrn-$(date +"%Y-%m-%d-%H-%M-%S").png

input * {
    xkb_layout "dk"
    #xkb_variant "pc105"
    #xkb_options "altwin:swap_lalt_lwin"
}

#input <identifier> xkb_model "macintosh79"

input <identifier> repeat_delay 300
input <identifier> repeat_rate 30
# On start
exec_always mako
exec_always kanshi
set $SWAYSOCK.wob "/tmp/wobpipe"
exec_always tail -f $SWAYSOCK.wob | wob -a center -a bottom -H 45 -W 300 -M 50 -o 4
exec mkfifo $SWAYSOCK.wob
exec wl-paste -t text --watch clipman store
exec wl-paste -p -t text --watch clipman store -P --histpath="~/.local/share/clipman-primary.json"
exec wl-paste -w 'wl-paste >> ~/.clipboard'
bindsym $mod+y exec tac ~/.clipboard | bemenu | wl-copy -n
set $term alacritty
#set $spotlight wofi --dmenu
#set $bar waybar
#set $launcher "wofi --show drun"
#set $menu exec alacritty --class tofi -d 80 10 -e sh -c '$HOME/.config/sway/scripts/tofi | sort -u -r | fzf | $HOME/.config/sway/scripts/tofi'
set $menu2 dmenu_path | wofi --show drun -i | xargs swaymsg exec --
set $menu3 bemenu-run | xargs swaymsg exec --
#set $pass "~/dotfiles/wofi/passwofi.sh"
#set $lock "swaylock -c 000000"

#include workspaces
include color_vars
include colors
include window_placement
include key_bindings
include inputs
include bar
include gtk_config



#include config.d/common.conf
set $backlight 'intel_backlight'
# Your preferred application launcher
# Note: pass the final command to swaymsg so that the resulting window can be opened
# on the original workspace that the command was run on.
# set $menu dmenu_path | dmenu | xargs swaymsg exec --

# set $menuu rofi -m $(expr $(swaymsg -t get_tree | jq '.nodes | map([recurse(.nodes[]?, .floating_nodes[]?) | .focused] | any) | index(true)') - 1) -show drun -run-command 'swaymsg exec -- {cmd}'
# set $menuuu rofi -m $(swaymsg -t get_outputs | jq 'map(select(.active) | .focused) | index(true)') -show drun -run-command 'swaymsg exec -- {cmd}'

for_window [app_id="tofi"] floating enable, border pixel 2, sticky enable

set $menu exec alacritty --class tofi -d 80 10 -e sh -c '$HOME/.scripts/launcher.sh | sort -u -r | fzf '




#bindsym $mod+f1 exec ~/.config/sway/lockman.sh
#bindsym $mod+f12 exec ~/.config/sway/lockman.sh

# Screenshot with grim and swappy
bindsym print exec grim -g "$(slurp)" - | swappy -f -
bindsym Shift+print exec grim - | swappy -f -


#exec ~/.local/bin/smartfloat tofi
#exec ~/.config/sway/scripts/wsdnames.py
### Output configuration
#
# Default wallpaper (more resolutions are available in /usr/share/backgrounds/sway/)
#output * bg /usr/share/backgrounds/sway/Sway_Wallpaper_Blue_1920x1080.png fill
output * bg ~/Downloads/sort1.jpg fill

#smart_gaps on
gaps inner 0
gaps outer 0

default_border pixel 0
default_floating_border normal 0
hide_edge_borders smart
#
# Example configuration:
#
   output eDP-1 resolution 2560x1440 position 2560,0

   for_window [title="^.*"] border pixel 1, title_format "%class >> %title"
#
# You can get the names of your outputs by running: swaymsg -t get_outputs
set $cl_high #ffd3d3
set $cl_indi #d9d8d8
set $cl_back #231f20
set $cl_fore #d9d8d8
set $cl_urge #ee2e24

#  class                border   bg       text     indi     childborder
client.focused          $cl_high $cl_high $cl_fore $cl_indi $cl_high
client.focused_inactive $cl_back $cl_back $cl_fore $cl_back $cl_back
client.unfocused        $cl_back $cl_back $cl_fore $cl_back $cl_back
client.urgent           $cl_urge $cl_urge $cl_fore $cl_urge $cl_urge
client.background       #ffffff


# monitors
	set $Mon1 eDP-1

### Idle configuration
#
# Example configuration:
#
#   exec swayidle -w \
#            timeout 1800 'media pause' \
#            timeout 1800 'swaylock -f -c 000000' \
#            timeout 900 'swaymsg "output * dpms off"' resume 'swaymsg "output * dpms on"' \
#            timeout 15 'if pgrep -x swaylock; then swaymsg "output * dpms off"; fi' \
#            resume 'swaymsg "output * dpms on"' \
#            before-sleep 'swaylock -f -c 000000'
#  #
font pango:Fira Code 18
# Autostart
exec mako --icons 1 --markup 1 --actions 1 --padding 10 --background-color "#81a1c1" --default-timeout 10000
exec light -S 20
exec amixer sset 'Master' 40%
exec nm-applet
exec nm-applet --indicator
exec /usr/lib/kdeconnectd
#exec_always /home/i/.scripts/swayinfo/wsdnames.sh
exec kdeconnect-indicator
exec syncthing -no-browser
exec ~/.scripts/autotiling.py
#exec ulauncher --hide-window
exec ~/.azotebg
# Output configuration:
# You can get the names of your outputs by running: swaymsg -t get_outputs

	output eDP-1 resolution 1920x1080 position 0,0
	output HDMI-A-1 resolution 1920x1080 position 1920,0

	# Turn off screen on lid closed
		set $laptop eDP-1
		bindswitch lid:on output $laptop disable
		bindswitch lid:off output $laptop enable


### Idle configuration

#set $menu nwggrid -o 0.9 -b 434c5e
bindsym $mod+backspace exec /bin/zsh -i -c wofer
# Fn key
bindsym XF86AudioRaiseVolume exec "pactl set-sink-volume @DEFAULT_SINK@ +5% ; zsh -c wob_volume > $SWAYSOCK.wob"
bindsym XF86AudioLowerVolume exec "pactl set-sink-volume @DEFAULT_SINK@ -5%; zsh -c wob_volume > $SWAYSOCK.wob"
bindsym XF86AudioMute exec "pactl set-sink-mute @DEFAULT_SINK@ toggle"
bindsym XF86MonBrightnessUp exec "brightnessctl set +10%"
bindsym XF86MonBrightnessDown exec "brightnessctl set 10%-"

## Screenshots
bindsym --release Print exec zsh -c screenshot
bindsym --release Shift+Print exec zsh -c fullscreenshot

# Split mod
bindsym $mod+b splith
bindsym $mod+v splitv

# Switch the current container between different layout styles
bindsym $mod+s layout stacking
bindsym $mod+t layout tabbed
bindsym $mod+n layout toggle split
#quick_back_and_forth_work_space
workspace_auto_back_and_forth yes
bindsym $mod+z workspace prev_on_output

bindsym $mod+Tab workspace back_and_forth
  
#bindsym $mod+Shift+Tab workspace next_on_output

#bindsym $mod+c exec clipman pick -t wofi

exec clipman pick --tool wofi -T'--prompt=my-prompt -i'

exec clipman pick --print0 --tool=CUSTOM --tool-args="fzf --prompt 'pick > ' --bind 'tab:up' --cycle --read0".


# Make the current focus fullscreen
bindsym $mod+f fullscreen

# Toggle the current focus between tiling and floating mode
bindsym $mod+Ctrl+space floating toggle

# Swap focus between the tiling area and the floating area
#bindsym $mod+space focus mode_toggle

# move focus to the parent container
bindsym $mod+a focus parent

# Scratchpad:
## Move the currently focused window to the scratchpad
#bindsym $mod+Shift+Tab move scratchpad

## If there are multiple scratchpad windows, this command cycles through them.
#bindsym $mod+Tab scratchpad show



# This will lock your screen after 300 seconds of inactivity, then turn off
# your displays after another 300 seconds, and turn your screens back on when
# resumed. It will also lock your screen before your computer goes to sleep.
### Input configuration
#
# Example configuration:
#
#   input "2:14:SynPS/2_Synaptics_TouchPad" {
#       dwt enabled
#       tap enabled
#       natural_scroll enabled
#       middle_emulation enabled
#   }
#
# You can get the names of your inputs by running: swaymsg -t get_inputs
# Read `man 5 sway-input` for more information about this section.
### Key bindings
#
# Basics:
#
    bindsym $mod+Print exec $screenshot
    bindsym $mod+Ctrl+Print exec $screenclip
    # Start a terminal
    bindsym $mod+m exec $term
    bindsym $mod+return exec $spotlight
    bindsym $mod+p exec $browser
    bindsym $mod+o exec $menu2
    bindsym $mod+space exec $menu
    bindsym space+return exec $menu2
    # Kill focused window
    bindsym $mod+Ctrl+q kill
    # Start your launcher
    # Drag floating windows by holding down $mod and left mouse button.
    # Resize them with right mouse button + $mod.
    # Despite the name, also works for non-floating windows.
    # Change normal to inverse to use left mouse button for resizing and right
    # mouse button for dragging.
    floating_modifier $mod normal
    # Reload the configuration file
    bindsym $mod+Ctrl+c reload
    # Exit sway (logs you out of your Wayland session)
    bindsym $mod+Ctrl+e exec swaynag -t warning -m 'You pressed the exit shortcut. Do you really want to exit sway? This will end your Wayland session.' -b 'Yes, exit sway' 'swaymsg exit'

	# Borders
    default_border none
    default_floating_border normal
    hide_edge_borders none
    popup_during_fullscreen smart


#
# Moving around:
#
    # Move your focus around
    bindsym $mod+$left focus left
    bindsym $mod+$down focus down
    bindsym $mod+$up focus up
    bindsym $mod+$right focus right
    # Or use $mod+[up|down|left|right]
    #bindsym $mod+Left focus left
    #bindsym $mod+Down focus down
    #bindsym $mod+Up focus up
    #bindsym $mod+Right focus right
    # Move the focused window with the same, but add Ctrl
    bindsym $mod+Ctrl+$left move left
    bindsym $mod+Ctrl+$down move down
    bindsym $mod+Ctrl+$up move up
    bindsym $mod+Ctrl+$right move right
    # Ditto, with arrow keys
    #bindsym $mod+Ctrl+Left move left
    #bindsym $mod+Ctrl+Down move down
    #bindsym $mod+Ctrl+Up move up
    #bindsym $mod+Ctrl+Right move right
#
# Workspaces:
#
# Workspace naming
# set $ws1 ""
# set $ws2 ""
# set $ws3 ""
# set $ws4  ""
# set $ws5  ""
# set $ws6  ""
# set $ws7 "focused": "",
# set $ws8  "default": ""
# set $ws7  "7:"
# set $ws8  "8:"
# set $ws9  "9:"
# set $ws10 "10:"


# Icons
# set $ws_mail "1:"
# set $ws_messaging "2:"
# set $ws_chromium "3:"
# set $ws_terminal "4:"
# set $ws_code "5:"
# set $ws_files "6:"
# set $ws_music "7:"


 #Icons
 set $ws_mail "1"
 set $ws_messaging "2"
 set $ws_chromium "3"
 set $ws_terminal "4"
 set $ws_code "5"
 set $ws_files "6"
 set $ws_music "7"

# disabled
#set $ws_chrome "999:"
#set $ws_firefox "999:"

set $ws_presAud "aud"
set $ws_presOwn "notes"

# Assign applications to specific workspaces
#ssign [class=$mail] $ws_mail
# assign [class=$music] $ws_music
# assign [class=$messaging] $ws_messaging
# assign [class=$mattermost] $ws_messaging
# assign [class=$xmpp] $ws_messaging
# assign [class=$chromium] $ws_chromium
# assign [class=$chrome] $ws_chrome
# assign [class=$firefox] $ws_firefox
# assign [class=$terminal] $ws_terminal
# assign [class=$sublime] $ws_code
# assign [class=$clion] $ws_code
# assign [class=$vscode] $ws_code
# assign [class=$nautilus] $ws_files

#
# set $ws1 "  "
#set $ws1 "  "
# set $ws3 "  "
# set $ws4 "  "
# set $ws5 "  "
# set $ws6 "  "
# set $ws7 "  "
# set $ws8 "  "
# set $ws9 "  "
# set $ws10 "  "
#assign [class="(?i)$browser"] → $ws3
#assign [class="(?i)$term"] → $ws1
#assign [class="(?i)nvim"] → $ws2


# Output configuration
# # workspace assignment
#   workspace 1 output $Mon1
#   workspace 2 output $Mon1
#   workspace 3 output $Mon1
#   workspace 4 output $Mon1
#   workspace 5 output $Mon1
#   workspace 6 output $Mon1
#   workspace 7 output $Mon1
#   workspace 8 output $Mon1
#
#  workspace $ws1 output primary
#  workspace $ws2 output primary
#  workspace $ws3 output primary
#  workspace $ws4 output primary
#  workspace $ws5 output primary
#  workspace $ws6 output primary
#  workspace $ws7 output primary
#  workspace $ws8 output primary
#  workspace $ws9 output primary
#  workspace $ws10 output primary
#
# Default workspace

#########workspace "  "

# Gaps and egdes

#set $focused "",
# switch to workspace
#bindsym $mod+1 workspace $ws_mail
#rename workspace [$ws_mail] to $ws1

# Workspaces:
#
  # workspace assignment
    workspace 1 output $Mon1
    workspace 2 output $Mon1
    workspace 3 output $Mon1
    workspace 4 output $Mon1
    workspace 5 output $Mon1
    workspace 6 output $Mon1
    workspace 7 output $Mon1
    workspace 8 output $Mon1

	# switch to workspace
		bindsym $mod+1 workspace number 1
		bindsym $mod+2 workspace number 2
		bindsym $mod+3 workspace number 3
		bindsym $mod+4 workspace number 4
		bindsym $mod+5 workspace number 5
		bindsym $mod+6 workspace number 6
		bindsym $mod+7 workspace number 7
		bindsym $mod+8 workspace number 8

	# move focused container to workspace
		bindsym $mod+Shift+1 move container to workspace number 1; workspace number 1
		bindsym $mod+Shift+2 move container to workspace number 2; workspace number 2
		bindsym $mod+Shift+3 move container to workspace number 3; workspace number 3
		bindsym $mod+Shift+4 move container to workspace number 4; workspace number 4
		bindsym $mod+Shift+5 move container to workspace number 5; workspace number 5
		bindsym $mod+Shift+6 move container to workspace number 6; workspace number 6
		bindsym $mod+Shift+7 move container to workspace number 7; workspace number 7
		bindsym $mod+Shift+8 move container to workspace number 8; workspace number 8

# bindsym $mod+1 workspace $ws_mail

# #########bindsym $mod+1 workspace $ws1
# #rename workspace [<old name>] to <new name>
# #swaymsg rename workspace 1 to firefox


# bindsym $mod+2 workspace $ws_messaging
# bindsym $mod+3 workspace $ws_chromium
# bindsym $mod+4 workspace $ws_terminal
# bindsym $mod+5 workspace $ws_code
# bindsym $mod+6 workspace $ws_files
# bindsym $mod+7 workspace $ws_music
# bindsym $mod+8 workspace 8
# bindsym $mod+9 workspace 9
# bindsym $mod+0 workspace 10

# # move focused container to workspace
# bindsym $mod+Control+1 move container to workspace $ws_mail
# bindsym $mod+Control+2 move container to workspace $ws_messaging
# bindsym $mod+Control+3 move container to workspace $ws_chromium
# bindsym $mod+Control+4 move container to workspace $ws_terminal
# bindsym $mod+Control+5 move container to workspace $ws_code
# bindsym $mod+Control+6 move container to workspace $ws_files
# bindsym $mod+Control+7 move container to workspace $ws_music
# bindsym $mod+Control+8 move container to workspace 8
# bindsym $mod+Control+9 move container to workspace 9
# bindsym $mod+Control+0 move container to workspace 10

#
# smart_gaps on
# smart_borders on
# gaps inner 8
# default_border pixel 2
# #focus_follows_mouse no
#     # Switch to workspace
#     bindsym $mod+1 workspace number 1
#     bindsym $mod+2 workspace number 2
#     bindsym $mod+3 workspace number 3
#     bindsym $mod+4 workspace number 4
#     bindsym $mod+5 workspace number 5
#     bindsym $mod+6 workspace number 6
#     bindsym $mod+7 workspace number 7
#     bindsym $mod+8 workspace number 8
#     bindsym $mod+9 workspace number 9
#     bindsym $mod+0 workspace number 10
#     # Move focused container to workspace
# #   bindsym $mod+Control+1 move container to workspace $ws1
# #   bindsym $mod+Control+2 move container to workspace $ws2
# #   bindsym $mod+Control+3 move container to workspace $ws3
# #   bindsym $mod+Control+4 move container to workspace $ws4
# #   bindsym $mod+Control+5 move container to workspace $ws5
# #   bindsym $mod+Control+6 move container to workspace $ws6
# #   bindsym $mod+Control+7 move container to workspace $ws7
# #   bindsym $mod+Control+8 move container to workspace $ws8
#     bindsym $mod+Control+9 move container to workspace $ws9
#     bindsym $mod+Control+0 move container to workspace $ws10
#     bindsym Control+Space exec makoctl dismiss
#     bindsym Control+Shift+Space exec makoctl dismiss --all
#     bindsym Ctrl+Alt+Right workspace next_on_output
#     bindsym Ctrl+Alt+$right workspace next_on_output
#     bindsym Ctrl+Alt+Left workspace prev_on_output
#     bindsym Ctrl+Alt+$left workspace prev_on_output
#
#     bindsym $mod+Control+1 move container to workspace number 1; workspace number 1
#     bindsym $mod+Control+2 move container to workspace number 2; workspace number 2
#     bindsym $mod+Control+3 move container to workspace number 3; workspace number 3
#     bindsym $mod+Control+4 move container to workspace number 4; workspace number 4
#     bindsym $mod+Control+5 move container to workspace number 5; workspace number 5
#     bindsym $mod+Control+6 move container to workspace number 6; workspace number 6
#     bindsym $mod+Control+7 move container to workspace number 7; workspace number 7
#     bindsym $mod+Control+8 move container to workspace number 8; workspace number 8
#
# #
    # Note: workspaces can have any name you want, not just numbers.
    # We just use 1-10 as the default.
# Specific window configuration
# Layout stuff:
#
    # You can "split" the current object of your focus with
    # $mod+b or $mod+v, for horizontal and vertical splits
    # respectively.
    # Switch the current container between different layout styles
    # Make the current focus fullscreen
    # Toggle the current focus between tiling and floating mode
    # Swap focus between the tiling area and the floating area
    # Move focus to the parent container
#
# Scratchpad:
#
    # Sway has a "scratchpad", which is a bag of holding for windows.
    # You can send windows there and get them back later.
    # Move the currently focused window to the scratchpad
    #bindsym $mod+Ctrl+z move scratchpad
    # Show the next scratchpad window or hide the focused scratchpad window.
    # If there are multiple scratchpad windows, this command cycles through them.
    #bindsym $mod+z scratchpad show
#
# Resizing containers:
#
#mode "resize" {
    # left will shrink the containers width
    # right will grow the containers width
    # up will shrink the containers height
    # down will grow the containers height
#     bindsym $left resize shrink width 10px
#     bindsym $down resize grow height 10px
#     bindsym $up resize shrink height 10px
#     bindsym $right resize grow width 10px
#     # Ditto, with arrow keys
#     bindsym Left resize shrink width 10px
#     bindsym Down resize grow height 10px
#     bindsym Up resize shrink height 10px
#     bindsym Right resize grow width 10px
#     # Return to default mode
#     bindsym Return mode "default"
#     bindsym Escape mode "default"
# }
# bindsym $mod+r mode "resize"

#bindsym $mod+n exec --no-startup-id flash_window
#
# Status Bar:
#
# Read `man 5 sway-bar` for more information about this section.
#
# Status Bar:
#
# Read `man 5 sway-bar` for more information about this section.
bar {
    font pango:Hack 16, Font Awesome 5,
    position top
    height 10
    swaybar_command waybar
 }
# bar {
#     position top
#     # When the status_command prints a new line to stdout, swaybar updates.
#     # The default just shows the current date and time.
#     status_command while date +'%Y-%m-%d %l:%M:%S %p'; do sleep 1; done
#     colors {
#         statusline #ffffff
#         background #323232
#         inactive_workspace #32323200 #32323200 #5c5c5c
#     }
# }

#include /etc/sway/config.d/*
# Hide cursor after one second of inactivity.
seat * hide_cursor 1000

# Hide titlebar, even while in tabbed mode if there is only a single window.
default_border none
#hide_edge_borders --i3 smart
#border_images.focused ~/.config/sway/borders/all.png
#border_images.focused_inactive ~/.config/sway/borders/all.png
#border_images.unfocused ~/.config/sway/borders/all.png
#border_images.urgent ~/.config/sway/borders/all.png

# Drag modifier
floating_modifier $mod

#bar {
#    status_command waybar
#}

for_window [title="^.*"] border pixel 1, title_format "%class >> %title"


input type:touchpad {
    tap enabled
    natural_scroll enabled
    dwt enabled
}


#include config.d/bindsym-common.conf
#include config.d/bindsym-normkbd.conf

exec_always xrdb -load ~/.Xresources
xwayland enable

	for_window [title="^.*"] border pixel 1, title_format "%class >> %title"

# Change gaps
#
	bindsym $mod+equal gaps inner current plus 5
	bindsym $mod+minus gaps inner current minus 5
	bindsym $mod+plus gaps outer current plus 5
	bindsym $mod+underscore gaps outer current minus 5




#
# application settings
#
       #for_window [class="(?i)(?:Steam|pavucontrol)"] floating enable, focus
       #for_window [class="(?i)(?:tilix|thunar|alacritty)"] focus
       ## for_window [class="(?i)(?:MellowPlayer)"] move scratchpad
       #for_window [title="MellowPlayer"] move scratchpad
       #for_window [class="(?i)(?:mousepad|Chromium|tint2conf)"] focus
       #for_window [class="(?i)(?:google-chrome)"] border none
       ## Assign Applications to specific workspace
       ## get class with xprop
       #assign [class="(?i)(?:tint2conf)|pamac-manager"] → workspace number 1
       #assign [class="(?i)(?:$browser)"] → workspace number 3
       #assign [class="(?i)(?:$editor)"] → workspace number 2
       #assign [class="(?i)(?:$term)"] → workspace number 1
       #assign [class="(?i)(?:Steam)"] → workspace number 4
       #assign [class="(?i)(?:gimp-*|inkscape)"] → workspace number 6
       #assign [title="gThumb"] → workspace number 6
       #assign [class="(?i)(?:game.py)"] → workspace number 3
       #assign [title="squarely"] → workspace number 2
       #assign [class="(?i)(?:jetbrains*|codeblocks)"] → workspace number 8
       ## focus, floating, & sticky
       #for_window [title="(?i)(?:copying|deleting|moving|catfish)"] floating enable, border none
       #for_window [class="(?i)(?:pavucontrol|termite|game.py|galculator|baobab|gnome-disks)"] floating enable, focus
       #for_window [window_role="(?i)(?:pop-up|setup|Arch)"] floating enable, focus
       #for_window [window_role="(?i)(?:browser)"] floating disable
       #for_window [title="Google Hangouts*"] resize set width 560 px
       #for_window [title="squarely*"] resize set height 870 px
       #for_window [class="(?i)(?:codeblocks|tilix|alacritty|jetbrains*)"] focus
       #for_window [title="Update*|Confirm*|Preferencje*|Sterowanie g*|Calendar*|pamac*"] floating enable
       #for_window [title="galculator*"] floating enable
       #for_window [title="Azote~"] floating enable, focus
       #for_window [title="Cheatsheet*"] floating enable, focus
       #for_window [title="feh*"] floating enable, focus
       #for_window [title="gThumb|Arch-update|Wybór daty*"] floating enable, focus
       #for_window [class="Chromium*"] floating disable, border none
       ## for_window [title="~sgtk*"] border none
# Assignment

#  assign [class="Slack"] → $ws4
#  assign [class="Riot"] → $ws4
#  assign [class="Postman"] → $ws7
#
#  assign [class="illnotes"] → $ws1
#  assign [instance="Navigator"] → $ws2
#  assign [class="Thunderbird"] → $ws6
#  assign [class="KeeWeb"] → $ws10
#
#  # IntelliJ
#
#  assign [class="jetbrains-idea" title="(.*)IntelliJ IDEA"] → $ws3
#  assign [class="sun-awt-X11-XFramePeer" class="jetbrains-idea"] → $ws3
#  assign [class="sun-awt-X11-XWindowPeer" class="java-lang-Thread"] → $ws3
#  for_window [class="sun-awt-X11-XWindowPeer" class="java-lang-Thread"] floating enable
#  for_window [class="jetbrains-idea" title="(.*)Default Changelist"] fullscreen enable
#
#  # Clion ##TODO##
#  assign [class="jetbrains-clion"] → $ws3
#  for_window [class="jetbrains-clion"] border none
#
#  for_window [class="Inkscape"] border none
#  for_window [app_id="org.gnome.Nautilus"] border csd


for_window [instance="Godot_Engine"] floating enable

include config.d/style.conf
include config.d/autostart.conf
#exec_always ~/.config/waybar/waybar.sh
exec light -S 20
exec amixer sset 'Master' 40%
exec obhud --touchpad off -c sway.conf

set $gnome-schema org.gnome.desktop.interface
exec_always {
    gsettings set $gnome-schema gtk-theme 'Nordic'
    gsettings set $gnome-schema icon-theme 'Nordic'
    gsettings set org.gnome.desktop.interface gtk-theme "Nordic"
    gsettings set org.gnome.desktop.wm.preferences theme "Nordic"
}
#exec autotiling
#exec_always ~/.scripts/swayinfo/wsdnames.sh
exec_always ~/.scripts/swayinfo/scratchpad_indicator.py 500
#exec_always flashfocus-git
exec redshift -m wayland
exec_always ~/.config/sway/scripts/compact-workspaces.py
exec_always ~/.scripts/wsdnames.sh
exec_always autotiling
exec clipman restore
#include ~/.config/sway/$(hostname)/*
